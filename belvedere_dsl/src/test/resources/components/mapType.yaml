package components

api {
    components {
        schema('Thing') {
            required schema('from': String)

            schema('tags': Map, mapType:String )

            schema('tag2':Map) {

                schema(code:int )
                schema(text:String)
            }
        }
    }
}